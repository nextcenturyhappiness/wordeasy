VITE_APP_MODE=cloud
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Desktop packaging (`vite --mode desktop`) reads the same two public values
# from this file, `.env.local`, `.env.production`, or CI secrets.
# Do not put a publishable key in the committed `.env.desktop` file.
